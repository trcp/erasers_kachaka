/**:
  ros__parameters:
    kr: 2.5            # 反発係数：障害物から受ける力の強さ。大きいほど障害物を強く避けようとする
    d0: 0.5            # 影響半径：この距離より遠い障害物は無視される（反発力0）
    max_angular: 1.5   # 最大角速度（[rad/s]）：反発トルクがこの値を超えないように制限
    valid_angle_min: -1.5708  # 有効角度下限（[rad]）：-π/2（前方-90度）から…
    valid_angle_max: 1.5708   # 有効角度上限（[rad]）：…+π/2（前方+90度）までのスキャンのみ考慮